import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/dashboard/_layout/manager/collect')({
  component: () => <div>Hello /dashboard/_layout/manager/collect!</div>,
})
