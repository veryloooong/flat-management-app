import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/dashboard/_layout/manager/add')({
  component: () => <div>Hello /dashboard/_layout/manager/add!</div>,
})
